<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ojk.animation.portfolio" >

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name=".fragmentmanaging.ui.BottomNavActivity"
            android:label="@string/title_activity_bottom_nav"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".bodycheck.BodyCheckDemoActivity" />
        <activity android:name=".viewoverlay.FoldAnimationActivity" />
        <activity android:name=".MainActivity" >
        </activity>
        <activity android:name=".DemoActivity" />
        <activity android:name=".youtubedemo.YouTubeDemoActivity" />
    </application>

</manifest>